@model st10251759_prog7311_ice_task_1.Models.ToDoTask

@{
    ViewData["Title"] = "Details";
}

<div class="container mt-5">
    <div class="card shadow-lg p-5 bg-light border-0">
        <h2 class="text-primary text-center fw-bold">Task Details</h2>
        <p class="lead text-center text-secondary">
            View the details of your task below.
        </p>

        <hr class="my-4 border-primary">

        <div class="row justify-content-center">
            <div class="col-md-8">
                <dl class="row">
                    <dt class="col-sm-4 fw-bold text-dark">
                        @Html.DisplayNameFor(model => model.Title)
                    </dt>
                    <dd class="col-sm-8 text-secondary">
                        @Html.DisplayFor(model => model.Title)
                    </dd>

                    <dt class="col-sm-4 fw-bold text-dark">
                        @Html.DisplayNameFor(model => model.Description)
                    </dt>
                    <dd class="col-sm-8 text-secondary">
                        @Html.DisplayFor(model => model.Description)
                    </dd>

                    <dt class="col-sm-4 fw-bold text-dark">
                        @Html.DisplayNameFor(model => model.DueDate)
                    </dt>
                    <dd class="col-sm-8 text-secondary">
                        @Html.DisplayFor(model => model.DueDate)
                    </dd>

                    <dt class="col-sm-4 fw-bold text-dark">
                        @Html.DisplayNameFor(model => model.IsCompleted)
                    </dt>
                    <dd class="col-sm-8 text-secondary">
                        @Html.DisplayFor(model => model.IsCompleted)
                    </dd>
                </dl>

                <hr class="my-4 border-primary">

                <div class="d-flex justify-content-center gap-3">
                    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning btn-lg shadow-sm">
                        <i class="bi bi-pencil-square"></i> Edit Task
                    </a>
                    <a asp-action="Index" class="btn btn-outline-secondary btn-lg shadow-sm">
                        <i class="bi bi-arrow-left"></i> Back to List
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

                <style>
                    /* General Styles */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f4f8; /* Light blue-gray background */
    color: #333;
    margin: 0;
    padding: 0;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

/* Card Styles */
.card {
    background: linear-gradient(135deg, #ffffff, #f0f8ff); /* Light white to light blue gradient */
    border-radius: 15px;
    padding: 40px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
}

/* Heading Styles */
h2 {
    color: #1a73e8; /* Primary blue for heading */
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 20px;
}

.lead {
    color: #4a5568; /* Dark gray-blue for subheading */
    font-size: 1.25rem;
    margin-bottom: 30px;
}

/* Horizontal Line */
hr {
    border: 0;
    height: 2px;
    background: #1a73e8; /* Blue for horizontal line */
    margin: 20px 0;
}

/* Definition List Styles */
dl.row {
    margin: 0;
}

dt {
    font-weight: 600;
    color: #1a73e8; /* Blue for definition terms */
    font-size: 1.1rem;
}

dd {
    color: #4a5568; /* Dark gray-blue for definition details */
    font-size: 1rem;
    margin-bottom: 15px;
}

/* Button Styles */
.btn {
    padding: 10px 20px;
    font-size: 1rem;
    border-radius: 6px;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 8px;
}

.btn-warning {
    background-color: #ffc107; /* Yellow for edit button */
    border: none;
    color: #000;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.btn-warning:hover {
    background-color: #e0a800; /* Darker yellow on hover */
    transform: translateY(-2px);
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
}

.btn-outline-secondary {
    background-color: transparent;
    border: 1px solid #6c757d; /* Gray for back button */
    color: #6c757d;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.btn-outline-secondary:hover {
    background-color: #6c757d; /* Gray on hover */
    color: #ffffff;
    transform: translateY(-2px);
    box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
}

/* Icon Styles */
.bi {
    font-size: 1.2rem;
}

                </style>